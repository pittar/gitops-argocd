apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cicd-dc1
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  project: cicd-project
  source:
    path: gitops/resources/cicd/project
    repoURL: git.url.git
    targetRevision: master
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
  sync:
    comparedTo:
      destination:
        namespace: argocd
        server: https://kubernetes.default.svc
      source:
        path: gitops/resources/cicd/project
        repoURL: git.url.git
        targetRevision: master
