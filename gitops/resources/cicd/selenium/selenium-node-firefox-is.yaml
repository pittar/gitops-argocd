apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  labels:
    app: selenium
    build: selenium-node-firefox
  name: selenium-node-firefox
  namespace: cicd
spec:
  lookupPolicy:
    local: false
