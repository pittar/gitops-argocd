apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  labels:
    app: selenium
    build: selenium-node-chrome
  name: selenium-node-chrome
  namespace: cicd
spec:
  lookupPolicy:
    local: false
